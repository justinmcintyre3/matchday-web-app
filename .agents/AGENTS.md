# Matchday Project Rules

## Build & Install Workflow

- **To build**: `flutter build apk --release` (run from project root)
- **To install**: Use `adb install -r "<apk_path>"` — the USER runs this command themselves. Do NOT run `flutter install`.
- APK output path is typically: `build\app\outputs\flutter-apk\app-release.apk`
